version: "3.9"

services:
  api:
    build: ./backend
    env_file:
      - backend/.env.example
    environment:
      - REDIS_URL=redis://redis:6379/0
    ports:
      - "8080:8080"
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
